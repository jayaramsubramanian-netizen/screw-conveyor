
# VECTRIX™ Screw Conveyor Designer — PySide6 desktop app
# Run: pip install -r requirements.txt
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.50
pydantic==2.7.1
python-multipart==0.0.9
PySide6>=6.7.0
pyqtgraph>=0.13.7      # interactive charts (line, bar, regime map)
numpy>=1.26.0          # pyqtgraph dependency + engineering arrays
matplotlib>=3.9.0      # PDF report figure generation only
requests>=2.32.0       # backend API calls (when backend exists)

